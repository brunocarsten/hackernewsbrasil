import {validFeeds} from '@/utils/api'

export default defineEventHandler((event) => {
    // const objUrl = getRequestURL(event);
    // console.log('server middleware: ' + objUrl)

    // if (!validFeeds.includes(objUrl as string)) {
    //     return navigateTo(`/${validFeeds[0]}/1`)
    // }
})